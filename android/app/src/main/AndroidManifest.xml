<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Rappels programmes (flutter_local_notifications).

        Ce fichier est volontairement versionne : `flutter create` (lance par
        .github/workflows/build.yml) genere le reste du dossier android/ mais
        ne remplace PAS un manifeste deja present. Sans ces declarations, le
        manifeste par defaut ne contient ni permission d'alarme ni receiver,
        et AUCUNE notification programmee ne se declenche.
    -->

    <!-- Android 13+ (API 33) : sans cette permission, aucune notification
         ne peut etre affichee, meme immediate. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Android 12+ (API 31) : necessaire pour les alarmes exactes, donc
         pour AndroidScheduleMode.exactAllowWhileIdle. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- Les alarmes AlarmManager sont perdues au redemarrage : le boot
         receiver ci-dessous les reprogramme, mais seulement s'il est
         autorise a recevoir BOOT_COMPLETED. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:label="MémoTack"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
            Receivers de flutter_local_notifications.

            C'est LE maillon manquant : zonedSchedule() programme une alarme
            AlarmManager qui diffuse vers ScheduledNotificationReceiver. Si ce
            receiver n'est pas declare ici, l'alarme se declenche dans le vide
            et la notification n'apparait jamais.
        -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>

        <!-- Reprogramme les rappels apres un redemarrage ou une mise a jour. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Gere les actions declenchees depuis la notification elle-meme. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false"/>
    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
